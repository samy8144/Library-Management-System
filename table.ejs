<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Management System</title>
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #ffffff, #ffffff);
            color: white;
        }

        .header {
            background-color: rgba(96, 84, 84, 0.8);
            display: flex;
            justify-content: space-around;
            align-items: center;
            padding: 1rem;
            color: rgb(132, 13, 13);
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .header div {
            
            padding: 1rem 1rem;
           
            
            text-align: center;
            transition: background-color 0.3s ease;
        }

        
        .header a {
            text-decoration: none;
            color: #fff;
            font-weight: bold;
            font-size: 1.2rem;
        }

        .container {
            margin: 2rem auto;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
            color: white;
            max-width: 90%;
        }

        .table {
            background-color: rgba(255, 255, 255, 0.8);
            color: #333;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
        }

        .table th {
            background-color: #918b94;
            color: white;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .table td {
            vertical-align: middle;
            padding: 1rem;
        }

        .table-hover tbody tr:hover {
            background-color: rgba(106, 27, 154, 0.1);
            transition: background 0.3s ease;
        }

        a {
            text-decoration: none;
            color: #6A1B9A;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #4A148C;
        }

        .no-records {
            color: #6A1B9A;
            font-size: 1.2rem;
            font-style: italic;
        }

        .add-user-button {
            display: block;
            margin: 2rem auto 0;
            padding: 0.8rem 2rem;
            background-color: #6A1B9A;
            color: white;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: transform 0.3s ease, background-color 0.3s ease;
            text-align: center;
        }

        .add-user-button a {
            text-decoration: none;
            color: white;
        }

        .add-user-button:hover {
            background-color: #4A148C;
            transform: scale(1.1);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        form {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            padding: 10px;
            border: 2px solid #ddd;
            border-radius: 8px;
            background-color: #f9f9f9;
            max-width: 100%;
            margin: 5 5;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        form h2 {
            width: 100%;
            font-family: Arial, sans-serif;
            font-size: 1rem;
            color: #333;
        }

        form input[type="search"] {
            flex: 1 1 calc(50% - 10px); 
            padding: 12px 12px;
            font-size: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        form input[type="search"]::placeholder {
            color: #aaa;
            font-style: italic;
        }
        .header1 {
            background-color: rgba(255, 255, 255, 0.8);
            display: flex;
            justify-content: space-around;
            align-items: center;
            padding: 1rem;
            color: rgb(132, 13, 13);
            font-size: 2rem;
            text-transform: uppercase;
            letter-spacing: 2px;}

        form input[type="submit"] {
            flex: 1 1 100%; 
            padding: 5px 15px;
            font-size: 1rem;
            color: white;
            background-color: #007BFF;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .table_head {
            display: flex;
            justify-content: space-around;
            align-items: center;
            padding: 1rem;
            color: rgb(132, 13, 13);
            font-size: 1.5rem;
            text-transform: uppercase;
            letter-spacing: 2px;}

        form input[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h2 class="header1">Library Management System</h2>
    <div class="header">
        <div><a href="/borrowings">loan</a></div>
        
        <div><a href="/home">Books</a></div>
        <div><a href="/users">Users</a></div>
    </div>
    
    <form action="/search">
        <h1 class="table_head">search</h1>
        <div>
            
            <input  name="book_id" type="number" placeholder="Enter book id" >
            </div>
            <br>
    <div><input name="title" type="text" placeholder="Enter book title"  >
    </div>
    
    <br>
    <div>
    <input name="author" type="text" placeholder="Enter book author" >
    </div>
<br>
    <div><input name="genre" type="text" placeholder="enter book genre"  ></div>
    <div><input name="number_of_copies" type="number" placeholder="enter no. of copies"  ></div>
    
    <div><input type="submit" value="search"></div>
    </form>
    <div class="container">
        <h1 class="table_head">list of Books</h1>
        <table class="table table-hover text-center">
            <thead>
                <tr>
                    <th>book_id</th>
                    <th>title</th>
                    <th>author</th>
                    <th>genre</th>
                    <th>copies</th>
                    <th>avail</th>
                    <th>update</th>
                </tr>
            </thead>
            <tbody>
                <% if (lib.length > 0) { %>
                    <% for (var i = 0; i < lib.length; i++) { %>
                        <tr>
                            <td><%- lib[i].book_id %></td>
                            <td><%- lib[i].title %></td>
                            <td><%- lib[i].author %></td>
                            <td><%- lib[i].genre %></td>
                            <td><%- lib[i].number_of_copies %></td>
                            <td><%- lib[i].availability %></td>
                            <td> 
                                <a href="/update_copy?book_id=<%- lib[i].book_id %>">update</a>
                              </td>
                              
                        </tr>
                    <% } %>
                <% } else { %>
                    <tr>
                        <td colspan="6" class="no-records">No records found.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
        <button class="add-user-button"><a href="log">Add new book</a></button>
    </div>
</body>
</html>
