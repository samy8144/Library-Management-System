<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Record</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: #f9f9f9;
            color: black;
        }

        .container {
            margin: 2rem auto;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 2rem;
            max-width: 500px;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
        }

        h2 {
            text-align: center;
            color: rgb(0, 0, 0);
            margin-bottom: 1.5rem;
        }

        label {
            color: rgb(0, 0, 0);
            font-weight: bold;
            margin-bottom: 0.5rem;
            display: block;
        }

        input[type="text"], input[type="email"], input[type="date"], input[type="submit"] {
            width: 100%;
            padding: 0.8rem;
            margin-bottom: 1rem;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
        }

        input[type="text"], input[type="email"], input[type="date"] {
            background-color: rgba(255, 255, 255, 0.9);
            color: #333;
        }

        input[type="submit"] {
            background-color: #6A1B9A;
            color: white;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #4A148C;
        }

        p {
            color: rgb(0, 0, 0);
            font-size: 1rem;
            text-align: center;
            margin-bottom: 1.5rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Update book copies</h2>

        <form action="" method="post">
                       <input name="book_id" value="<%= lib[0].book_id %>" type="hidden" >

            <div>
                <label for="name">Enter the total number of book copies</label>
                <input id="number_of_copies" value="<%= lib[0].number_of_copies %>" name="number_of_copies" type="text" placeholder="Enter copies books" required>
            </div>

           
            <div>
                <input type="submit" value="Update">
            </div>
        </form>
    </div>
</body>
</html>
